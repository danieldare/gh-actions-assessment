name: 01 - Building Blocks
on: push

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Multi-line bash
        run: |
          echo "I am"
          echo "a multi-line"
          echo "script
  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Failed step
        run: |
          echo "I will fail"
          exit 1
      - name: Say Goodbye
        run: echo "Goodbye"
