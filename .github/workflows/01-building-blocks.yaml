name: 01 - Building Blocks
on: push

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Multi-line bash
        run: |
          echo "I am"
          echo "a multi-line"
          echo "script"
  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Sucess step
        run: |
          echo "I will not fail"
          exit 0;
      - name: Say Goodbye
        run: echo "Goodbye"
